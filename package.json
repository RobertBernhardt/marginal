{
  "name": "marginal-tasker",
  "version": "1.0.0",
  "description": "Marginal utility based task-choosing system with Google Chat integration.",
  "main": "index.js",
  "scripts": {
    "login": "clasp login",
    "push": "clasp push",
    "watch": "clasp push --watch",
    "deploy": "clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^3.3.0"
  }
}
